type Query {
    recommendCourses(selectedCourse: [CourseEntryInput!]!): [CourseEntry!]!
}

input CourseEntryInput {
    courseId: String!
    studyProgram: String!
}

type CourseEntry {
    courseId: String!
    studyProgram: String!
}